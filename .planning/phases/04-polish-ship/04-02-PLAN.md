---
phase: 04-polish-ship
plan: 02
type: execute
depends_on: ["04-01"]
files_modified: [src/assets/icon-16.png, src/assets/icon-32.png, src/assets/icon-48.png, src/assets/icon-96.png, src/assets/icon-128.png, wxt.config.ts]
---

<objective>
Create extension icons, update manifest metadata, and prepare store submission assets.

Purpose: Make extension ready for Firefox Add-ons and Chrome Web Store submission.
Output: Complete icon set, proper manifest, and store listing text ready.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@wxt.config.ts

**Store requirements:**
- Chrome: icons at 16, 32, 48, 128px
- Firefox: icons at 16, 32, 48, 96px (96 preferred over 128)
- Both: compelling description, proper permissions justification

**Current state:** No icons in src/assets/. Manifest has placeholder gecko ID.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create extension icon SVG and PNG exports</name>
  <files>src/assets/icon.svg, src/assets/icon-16.png, src/assets/icon-32.png, src/assets/icon-48.png, src/assets/icon-96.png, src/assets/icon-128.png</files>
  <action>
Create a simple, recognizable icon for the extension:
1. Create `src/assets/icon.svg` with a clean design:
   - Red/YouTube-ish background circle or rounded square
   - White "GIF" text or film/play icon inside
   - Simple enough to be recognizable at 16px
2. Export to required PNG sizes using ImageMagick or similar:
   ```bash
   # After creating SVG, convert to PNGs
   # Use rsvg-convert if available, or online converter
   ```

Design should be simple and iconic. A red rounded rectangle with white "GIF" text works well.

Note: If ImageMagick/rsvg not available, create a checkpoint for manual icon generation.
  </action>
  <verify>All icon files exist: `ls -la src/assets/icon*.png`</verify>
  <done>icon.svg and all PNG sizes (16, 32, 48, 96, 128) exist in src/assets/</done>
</task>

<task type="auto">
  <name>Task 2: Update manifest with icons and final metadata</name>
  <files>wxt.config.ts</files>
  <action>
Update wxt.config.ts manifest configuration:
1. Add icons configuration for all sizes:
   ```typescript
   icons: {
     16: 'assets/icon-16.png',
     32: 'assets/icon-32.png',
     48: 'assets/icon-48.png',
     96: 'assets/icon-96.png',
     128: 'assets/icon-128.png',
   },
   ```
2. Update gecko.id to a proper format: `{uuid}` or `name@domain` (keep current format but ensure it's unique)
3. Ensure description is store-ready: "Create animated GIFs from any YouTube video. Select your time range, adjust quality, and download instantly."

WXT will automatically include appropriate icons per browser.
  </action>
  <verify>Build passes and includes icons: `pnpm build && ls .output/chrome-mv3/icons/`</verify>
  <done>Manifest includes icon paths, description is polished, gecko.id is proper format</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Extension icons and manifest configuration for store submission</what-built>
  <how-to-verify>
    1. Run: `pnpm dev` (or `pnpm dev:firefox`)
    2. Open browser extensions page (chrome://extensions or about:debugging)
    3. Check the extension shows the new icon (not default puzzle piece)
    4. Verify icon is clear and recognizable at small size
    5. Check extension description in manifest
  </how-to-verify>
  <resume-signal>Type "approved" if icon looks good, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `pnpm build` succeeds without errors
- [ ] `pnpm build:firefox` succeeds without errors
- [ ] Icons visible in both Chrome and Firefox builds
- [ ] Extension loads with proper icon in browser
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Extension has recognizable icon at all sizes
- Manifest ready for store submission
- Phase 4 complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-ship/04-02-SUMMARY.md` with:
- Icon design decisions
- Store submission readiness checklist
- Any remaining steps for actual submission (manual process)
</output>
